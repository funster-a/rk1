# 🚀 Как запустить приложение локально в Cursor

## 📋 Шаг 1: Установите необходимые расширения

1. Нажмите `Ctrl+Shift+X` (открыть Extensions)
2. Установите эти 4 расширения (обязательно):
   - **Kotlin Language** (fwcd.kotlin)
   - **Gradle Language Support** (naco-siren.gradle-language-support)
   - **XML** (redhat.vscode-xml)
   - **Error Lens** (usernamehw.errorlens)

3. Перезапустите Cursor после установки

## 📱 Шаг 2: Подготовьте Android устройство или эмулятор

### Вариант A: Физическое устройство (рекомендуется)

1. **Включите режим разработчика на телефоне:**
   - Настройки → О телефоне → Нажмите 7 раз на "Номер сборки"
   - Вернитесь в Настройки → Параметры разработчика
   - Включите "Отладка по USB"

2. **Подключите телефон к компьютеру через USB**

3. **Разрешите отладку по USB** (на телефоне появится запрос)

### Вариант B: Эмулятор Android

1. Установите Android Studio (только для эмулятора)
2. Создайте виртуальное устройство через AVD Manager
3. Запустите эмулятор

## 🔧 Шаг 3: Установите ADB (Android Debug Bridge)

### Windows (PowerShell):

1. **Скачайте Platform Tools:**
   - Перейдите: https://developer.android.com/studio/releases/platform-tools
   - Скачайте ZIP для Windows
   - Распакуйте в папку (например: `C:\Android\platform-tools`)

2. **Добавьте в PATH:**
   - Нажмите `Win + R` → введите `sysdm.cpl` → Enter
   - Вкладка "Дополнительно" → "Переменные среды"
   - В "Системные переменные" найдите `Path` → "Изменить"
   - "Создать" → добавьте путь: `C:\Android\platform-tools`
   - OK → OK → OK

3. **Проверьте установку:**
   - Откройте новый терминал в Cursor (`Ctrl+``)
   - Выполните:
     ```powershell
     adb version
     adb devices
     ```
   - Должно показать ваше устройство или эмулятор

## 🏗️ Шаг 4: Соберите приложение

В терминале Cursor (`Ctrl+``) выполните:

```powershell
# Перейдите в корень проекта (если не там)
cd C:\Users\Acer\rk1

# Соберите Debug APK
.\gradlew.bat assembleDebug
```

**Или используйте встроенные задачи:**
- Нажмите `Ctrl+Shift+P`
- Введите "Tasks: Run Task"
- Выберите "Gradle: Build Debug APK"

## 📲 Шаг 5: Установите приложение на устройство

### Способ 1: Через Gradle (проще всего)

```powershell
.\gradlew.bat installDebug
```

Эта команда автоматически:
- Соберет APK
- Установит на подключенное устройство
- Запустит приложение

### Способ 2: Вручную через ADB

```powershell
# Установите APK
adb install -r app\build\outputs\apk\debug\app-debug.apk

# Запустите приложение
adb shell am start -n com.example.rk1/.WelcomeActivity
```

## 🎯 Шаг 6: Запустите приложение

### Автоматический запуск:
```powershell
.\gradlew.bat installDebug
```
(Приложение запустится автоматически после установки)

### Ручной запуск:
```powershell
adb shell am start -n com.example.rk1/.WelcomeActivity
```

## 🔍 Шаг 7: Просмотр логов (для отладки)

В отдельном терминале:

```powershell
# Все логи
adb logcat

# Только логи вашего приложения
adb logcat | Select-String "rk1"

# Очистить и показать новые логи
adb logcat -c && adb logcat
```

## ⚡ Быстрые команды (создайте алиасы)

Добавьте в PowerShell профиль для удобства:

```powershell
# Откройте профиль
notepad $PROFILE

# Добавьте эти функции:
function Build-Android {
    Set-Location C:\Users\Acer\rk1
    .\gradlew.bat assembleDebug
}

function Install-Android {
    Set-Location C:\Users\Acer\rk1
    .\gradlew.bat installDebug
}

function Run-Android {
    adb shell am start -n com.example.rk1/.WelcomeActivity
}

function Logcat-Android {
    adb logcat | Select-String "rk1"
}

function Clean-Android {
    Set-Location C:\Users\Acer\rk1
    .\gradlew.bat clean
}
```

Теперь можно использовать:
- `Build-Android` - собрать
- `Install-Android` - установить и запустить
- `Run-Android` - запустить
- `Logcat-Android` - логи

## 🐛 Решение проблем

### Проблема: "adb не распознается как команда"
**Решение:**
- Проверьте, что ADB добавлен в PATH
- Перезапустите терминал Cursor
- Выполните: `$env:Path` (должен содержать путь к platform-tools)

### Проблема: "device not found" или "no devices/emulators found"
**Решение:**
- Проверьте USB-подключение
- Убедитесь, что включена отладка по USB
- Выполните: `adb devices` (должно показать устройство)
- Если пусто - переподключите USB или перезапустите ADB: `adb kill-server && adb start-server`

### Проблема: "Gradle build failed"
**Решение:**
- Проверьте Java: `java -version` (должна быть 11+)
- Очистите сборку: `.\gradlew.bat clean`
- Попробуйте снова: `.\gradlew.bat assembleDebug`

### Проблема: "INSTALL_FAILED"
**Решение:**
- Удалите старое приложение с телефона
- Или установите с перезаписью: `adb install -r app\build\outputs\apk\debug\app-debug.apk`

## 📝 Чек-лист перед запуском

- [ ] Установлены расширения Kotlin, Gradle, XML
- [ ] ADB установлен и добавлен в PATH
- [ ] Устройство подключено и видно в `adb devices`
- [ ] Java 11+ установлена
- [ ] Проект открыт в Cursor

## 🎉 Готово!

После выполнения всех шагов ваше приложение должно:
1. Собраться без ошибок
2. Установиться на устройство
3. Автоматически запуститься

**Для повторного запуска после изменений:**
```powershell
.\gradlew.bat installDebug
```

---

**Совет:** Используйте встроенные задачи Cursor (`Ctrl+Shift+P` → "Tasks: Run Task") для быстрого доступа к командам сборки!

